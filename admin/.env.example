# Backend manzili — /api/admin shu yerdan olinadi.
# Ishlayotgan backend: Cloudflare Worker (cloudflare-worker/).
NEXT_PUBLIC_API_URL=https://saler-api.akobirnorqulov104.workers.dev

# Panelga kirish paroli Worker Secret sifatida turadi, bu yerda emas:
#   cd cloudflare-worker && npx wrangler secret put ADMIN_PASSWORD
